import { Root, AvatarRootProps } from './Root'
import { Image, AvatarImageProps } from './Image'
import { Fallback, AvatarFallbackProps } from './Fallback'

const Avatar = { Root, Image, Fallback }

export { Avatar }

export type { AvatarRootProps, AvatarImageProps, AvatarFallbackProps }
