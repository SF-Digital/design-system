import { Root, TextInputRootProps } from './Root'
import { Control, TextInputControlProps } from './Control'

export const TextInput = {
	Root,
	Control,
}

export type { TextInputRootProps, TextInputControlProps }
